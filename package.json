{
  "name": "@yefrinoscar/tokens",
  "version": "0.1.0",
  "description": "Design tokens for Claro design system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "style": "dist/css/tokens.css",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yefrinoscar/tokens.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "build": "npm run clean && npm run build:tokens && npm run build:ts",
    "build:tokens": "node build.js",
    "build:ts": "tsc",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "design-tokens",
    "css",
    "style-dictionary",
    "claro"
  ],
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "style-dictionary": "^3.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
